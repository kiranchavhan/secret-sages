import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  paper: {
    margin: '2rem',
    padding: '1.5rem',
    minHeight: '70rem',
  },
}));

export default useStyles;
