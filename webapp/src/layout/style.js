import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  wrapper: {
    display: 'flex',
    width: '100%',
  },
  main: {
    width: '100%',
    margin: '7.9rem 1.5rem 1.5rem',
  },
}));

export default useStyles;
