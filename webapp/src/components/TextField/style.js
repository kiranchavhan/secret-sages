import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  startIconPadding: {
    paddingLeft: '1rem',
  },
  endIconPadding: {
    paddingRight: '1rem',
  },
}));

export default useStyles;
