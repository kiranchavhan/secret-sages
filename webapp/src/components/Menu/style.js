import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  iconWidth: {
    minWidth: 0,
  },
  iconPadding: {
    paddingRight: '1.6rem',
  },
  menuList: {
    padding: 0,
  },
}));

export default useStyles;
